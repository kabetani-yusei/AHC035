g++ main.cpp -std=c++17 -I .

#pragma GCC target("avx2")
#pragma GCC optimize("O3")
#pragma GCC optimize("unroll-loops")


短期ヒューリスティック(意識することまとめ)
方針を変えるのならば必ず提出する
score.pyを動かした時に時間を結び付けておいて、スコアとコードを結び付ける
nとかが固定されている問題が多いから、分かりやすくする
githubでバージョン管理をする